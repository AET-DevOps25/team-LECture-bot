# --- Application Settings ---
APP_NAME="GenAI Module for LECture-bot (Local Dev)"
GENAI_PORT=8001

# --- Weaviate Vector Database Configuration ---
# WEAVIATE_URL="http://localhost:8080" # Example if Weaviate is running locally on default port
# WEAVIATE_API_KEY="your_weaviate_api_key_if_needed" # Only if your Weaviate instance uses API key auth

# --- LLM Configuration (Example for OpenAI) ---
# OPENAI_API_KEY="your_actual_openai_api_key_here"
# EMBEDDING_MODEL_NAME="sentence-transformers/all-MiniLM-L6-v2" # Or another model

# --- Other Configurations as needed ---
# For example, paths to local LLM models if used, etc.