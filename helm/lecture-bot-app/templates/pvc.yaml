apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lecturebot-db-pvc
  namespace: "{{ .Values.namespacename }}"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

