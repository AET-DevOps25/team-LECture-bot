# Server Configuration
server.port=8080

# Spring Application Name
spring.application.name=lecturebot-server

# Database Configuration (Placeholders - to be configured via environment variables or profiles)
# spring.datasource.url=jdbc:postgresql://localhost:5432/lecturebot_db
# spring.datasource.username=lecturebot_user
# spring.datasource.password=secret
# spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration (Optional - sensible defaults are often sufficient)
# spring.jpa.hibernate.ddl-auto=update # Be cautious with 'create' or 'create-drop' in production
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.format_sql=true

# Logging Configuration (Example - Spring Boot default is usually good to start)
# logging.level.root=INFO
# logging.level.com.lecturebot=DEBUG
# logging.file.name=logs/lecturebot-server.log

# Spring Security (Basic - will need more configuration for JWT, etc.)
# spring.security.user.name=user # Default in-memory user (for very basic testing, remove for real auth)
# spring.security.user.password=password # Default in-memory user password

# Actuator Endpoints (for health, metrics, etc. - useful for monitoring)
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=when_authorized # or 'always' for easier local debugging
